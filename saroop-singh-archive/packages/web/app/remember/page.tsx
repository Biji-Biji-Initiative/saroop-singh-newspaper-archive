import type { Metadata } from "next";
import Link from "next/link";
import { Heart, LockKeyhole, ShieldCheck } from "lucide-react";
import { redirect } from "next/navigation";
import { MemoryRoom } from "./memory-room";

export const metadata: Metadata = { title: "Family Memory Room", description: "Privately add names, stories, corrections, photographs and voice memories to the Saroop Singh Archive." };
export default async function RememberPage({ searchParams }: { searchParams: Promise<{ subject?: string; kind?: string }> }) { const query = await searchParams; if (query.kind === "photograph") redirect("/contribute"); return <main className="min-h-screen bg-[#f6f1e8] text-[#17241d]"><section className="bg-[#17241d] text-[#f8f1e4]"><div className="mx-auto max-w-6xl px-5 py-14 sm:px-8 sm:py-20"><p className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.22em] text-amber-300"><Heart className="h-4 w-4" /> Family Memory Room</p><h1 className="mt-5 max-w-5xl font-serif text-6xl leading-[.88] sm:text-8xl">Return a name.<br />Preserve a voice.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-stone-300">Choose one small thing you know. No account and no password. Every contribution remains private until reviewed.</p><div className="mt-8 flex flex-wrap gap-3 text-sm text-stone-300"><span className="flex items-center gap-2 rounded-full bg-white/10 px-4 py-2"><LockKeyhole className="h-4 w-4" /> Private by default</span><span className="flex items-center gap-2 rounded-full bg-white/10 px-4 py-2"><ShieldCheck className="h-4 w-4" /> Claims, not automatic facts</span></div><Link href="/mysteries" className="mt-6 inline-flex min-h-11 items-center text-sm font-semibold text-amber-300 underline decoration-amber-300/40 underline-offset-4">Browse unidentified family photographs</Link></div></section><MemoryRoom initialSubject={query.subject} initialKind={query.kind} /></main>; }
