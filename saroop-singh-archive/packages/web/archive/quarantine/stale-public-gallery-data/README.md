# Stale public gallery metadata

These JSON copies were publicly served by the earlier build but were not used by
the current application. They contain AI-first thumbnails and unsupported dates
or titles that conflict with the reviewed curation layer.

They are quarantined for audit only. `data/gallery` plus
`lib/gallery-curation.ts` currently drive the recovered legacy catalogue until a
single structured provenance schema replaces both.
